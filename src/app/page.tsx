"use client"; import { useState } from "react"; import { motion, AnimatePresence } from "framer-motion"; import SearchForm from "@/components/SearchForm"; import CharacterCard from "@/components/CharacterCard"; import { charactersData, Character } from "@/data/mockData"; import { Sparkles, SpaceIcon as Calendar, Gift } from "lucide-react"; import Link from "next/link"; export default function Home() { const [results, setResults] = useState<{ anime: Character[], celebrity: Character[] } | null>(null); const handleSearch = (day: string, month: string) => { const searchDate = `${day}/${month}`; const matches = charactersData.filter(char => char.birthDate === searchDate); setResults({ anime: matches.filter(c => c.type === "anime"), celebrity: matches.filter(c => c.type === "celebrity") }); }; return ( <main className="min-h-screen bg-pastel-cream overflow-hidden font-quicksand pb-20 relative"> <div className="absolute top-[-10%] left-[-10%] w-96 h-96 bg-pastel-pink rounded-full mix-blend-multiply filter blur-3xl opacity-50 animate-pulse"></div> <div className="absolute top-[20%] right-[-10%] w-96 h-96 bg-pastel-blue rounded-full mix-blend-multiply filter blur-3xl opacity-50 animate-pulse" style={{animationDelay: '2s'}}></div> <div className="absolute bottom-[-10%] left-[20%] w-96 h-96 bg-pastel-yellow rounded-full mix-blend-multiply filter blur-3xl opacity-50 animate-pulse" style={{animationDelay: '4s'}}></div> <div className="relative z-10 max-w-5xl mx-auto px-4 pt-16 flex flex-col items-center"> <motion.div initial={{ y: -50, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.8, type: "spring" }} className="text-center mb-12" > <div className="inline-block bg-white/80 backdrop-blur px-6 py-2 rounded-full border border-pastel-pink mb-6 shadow-sm"> <span className="flex items-center gap-2 text-blue-400 font-bold tracking-widest uppercase text-sm"> <Sparkles size={16} /> Welcome to Birthday Universe <Sparkles size={16} /> </span> </div> <h1 className="text-5xl md:text-7xl font-extrabold text-gray-800 mb-6 drop-shadow-sm"> Hôm Nay Là <span className="text-transparent bg-clip-text bg-gradient-to-r from-pink-400 to-pastel-blue">Sinh Nhật</span> Ai? </h1> <p className="text-lg md:text-xl text-gray-600 max-w-2xl mx-auto font-medium"> Khám phá những nhân vật anime huyền thoại và vĩ nhân thế giới chia sẻ cùng ngày sinh với bạn! </p> </motion.div> <div className="w-full max-w-lg mb-12"> <SearchForm onSearch={handleSearch} /> </div> <div className="flex flex-wrap justify-center gap-4 mb-20 w-full px-4"> <Link href="/create-countdown" className="bg-white px-6 py-4 rounded-2xl shadow-lg flex items-center gap-3 font-bold text-gray-700 hover:scale-105 transition-transform border border-gray-100 w-full sm:w-auto justify-center"> <Calendar size={24} className="text-pastel-blue" /> Tạo Đếm Ngược </Link> <Link href="/create-gift" className="bg-white px-6 py-4 rounded-2xl shadow-lg flex items-center gap-3 font-bold text-gray-700 hover:scale-105 transition-transform border border-gray-100 w-full sm:w-auto justify-center"> <Gift size={24} className="text-pastel-pink" /> Tạo Quà Ảo </Link> </div> <AnimatePresence> {results && ( <motion.div initial={{ opacity: 0, y: 50 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, scale: 0.9 }} transition={{ duration: 0.5 }} className="w-full" > {results.anime.length === 0 && results.celebrity.length === 0 ? ( <div className="text-center bg-white/50 p-10 rounded-[2rem] border border-dashed border-gray-300 max-w-lg mx-auto"> <p className="text-xl text-gray-600 font-medium"> Oops! Hiện chưa có dữ liệu nhân vật nào sinh vào ngày này trong vũ trụ của chúng ta. 🛸 </p> </div> ) : ( <div className="flex flex-col gap-16 w-full"> {results.anime.length > 0 && ( <section className="flex flex-col items-center"> <div className="bg-pastel-pink/20 px-8 py-3 rounded-full mb-8"> <h2 className="text-3xl font-bold text-gray-800 flex items-center gap-3"> 🌟 Nhân Vật Anime </h2> </div> <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 w-full justify-items-center"> {results.anime.map(char => <CharacterCard key={char.id} character={char} />)} </div> </section> )} {results.celebrity.length > 0 && ( <section className="flex flex-col items-center"> <div className="bg-pastel-blue/20 px-8 py-3 rounded-full mb-8"> <h2 className="text-3xl font-bold text-gray-800 flex items-center gap-3"> 🌎 Người Nổi Tiếng </h2> </div> <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 w-full justify-items-center"> {results.celebrity.map(char => <CharacterCard key={char.id} character={char} />)} </div> </section> )} </div> )} </motion.div> )} </AnimatePresence> </div> </main> ); } 